# Data Visualization Agent

A multi-agent system for:
- Parsing experimental data (Excel/MATLAB CSV)
- Automatic plotting (high-resolution, publication-ready)
- Multi-subplot layout
- Auto caption + analysis generation

## Usage
```bash
pip install -r requirements.txt
python main.py --input data.xlsx
```
